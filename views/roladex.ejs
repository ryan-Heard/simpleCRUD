<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" rel="stylesheet">
    <script href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.4.7/angular.min.js"></script>
    <script src="/javascripts/main.js"></script>
</head>

<body>

<span id="title" class="h1">Roladex</span>

<div class="container" ng-app="directory">
    <div ng-controller="cardsCtrl">
        <div class="form-group">
            <form role="form" class="form">
                <input class="form-control" placeholder="Search" type="text" ng-model="query" maxlength="50" >
                <span class="glyphicon glyphicon-search" ng-click="find()" title="Search"></span>
                <span class="glyphicon glyphicon-plus pull-right" ng-click="addNew()" title="Add New Contacts"></span>
                <span class="glyphicon glyphicon-list-alt pull-right" ng-click="getAllContacts()" title="Show All Contacts"></span>
            </form>
        </div>

        <table class="table main table-striped">
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>City</th>
                <th>Zip Code</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
            <tr ng-repeat="person in persons" ng-hide="person.isDeleted">
                <td><input type="text" ng-model="person.firstName" ></td>
                <td><input type="text" ng-model="person.lastName"></td>
                <td><input type="text" ng-model="person.city"></td>
                <td><input type="text" ng-model="person.zipCode"></td>
                <td>
                    <span class="glyphicon glyphicon-plus pull-right"  ng-show="checkEmpty(person)" ng-click="addContact(person)" title="Add New Contact"></span>.
                     <span class="glyphicon glyphicon-pencil" ng-hide="checkEmpty(person)" ng-click="updateContact(person._id,person)" title="Update Contact"></span>
                </td>
                <td>
                    <span class="glyphicon glyphicon glyphicon-remove pull-right"  ng-click="deleteContact(person)" title="Delete Contact"></span>
                </td>
            </tr>
        </table>
    </div>
</div>

</body>
</html>